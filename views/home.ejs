<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
</head>
<body>
    <%- include('./partials/navbar') %>
    
    <div class="container">
        <% blogs.forEach(blog => {%>
            <div class="card">
                <h1 style="font-weight: bold;"><%= blog.title %></h1>
                <img src="../public/uploads/">
                <a href="/blogs/<%=user._id%>"><button>View</button></a>
            </div>
        <% }); %>
    </div>
</body>
</html>